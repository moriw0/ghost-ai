# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

- Phase 1: Foundation

## Current Goal

- Canvas foundation (React Flow setup, node/edge types).

## Completed

- Context files added (project overview, architecture, UI context, code standards, AI workflow rules).
- **01-design_system**: shadcn/ui configured with dark theme tokens in `globals.css`; Button, Card, Dialog, Input, Tabs, Textarea, ScrollArea added to `components/ui/`; lucide-react installed; `lib/utils.ts` with `cn()` created. Build verified clean.
- **02-editor-chrome**: `EditorNavbar` and `ProjectSidebar` created in `components/editor/`. Navbar has fixed-height top bar with PanelLeftOpen/PanelLeftClose toggle. Sidebar floats as overlay (no content push), slides in from left, has My Projects/Shared tabs with empty states and full-width New Project button. Dialog pattern already in place via `components/ui/dialog.tsx` with correct color tokens.
- **03-auth**: Clerk wired into the app. `ClerkProvider` with `dark` theme wraps root layout; appearance variables reference CSS custom properties from `globals.css`. `proxy.ts` at project root protects all routes except sign-in/sign-up (env-var-driven). Sign-in and sign-up pages at `/sign-in` and `/sign-up` use a minimal two-panel layout (logo + tagline + feature list on the left, Clerk form on the right; form-only on small screens). Root `/` redirects authenticated users to `/editor` and unauthenticated to `/sign-in`. `UserButton` added to the navbar right section. `@clerk/ui` installed.
- **04-project-dialogs**: Editor home screen with heading, description, and New Project button. Create/Rename/Delete project dialogs. `useProjectDialogs` hook manages dialog, form, and loading state. Sidebar shows mock project items with rename/delete actions for owned projects only; shared projects show read-only. Mobile backdrop scrim added. Mock data in `lib/mock-projects.ts` with `nameToSlug` utility. Build and TypeScript verified clean.
- **05-prisma**: `prisma/models/project.prisma` added with `Project` and `ProjectCollaborator` models, `ProjectStatus` enum (DRAFT/ARCHIVED), cascade delete, indexes on ownerId/createdAt/email. `lib/prisma.ts` exports a cached singleton branching on `DATABASE_URL`: `prisma+postgres://` uses Accelerate (`@prisma/extension-accelerate`), otherwise uses `PrismaPg` adapter. Migration `20260627031319_init` applied. Build verified clean.
- **06-project-apis**: REST API routes for projects. `GET /api/projects` lists owner's projects. `POST /api/projects` creates with optional name (defaults to "Untitled Project"). `PATCH /api/projects/[projectId]` renames (owner only). `DELETE /api/projects/[projectId]` deletes (owner only). All routes enforce auth via Clerk `auth()`: unauthenticated returns 401, non-owner mutations return 403. `lib/prisma.ts` type updated to cast the union client to `PrismaClient` for consistent TypeScript signatures. Build verified clean.
- **07-wire-editor-home**: Editor home wired to real project API. `lib/projects.ts` added with `getOwnedProjects(userId)` and `getSharedProjects(email)` server helpers. `app/editor/page.tsx` converted to server component — fetches owned and shared projects via `currentUser()` and passes to `EditorHome`. `components/editor/editor-home.tsx` extracted as the client component holding sidebar + dialog state. `hooks/use-project-actions.ts` created: create calls `POST /api/projects` with a slugified name + short random suffix as the room ID (project ID and room ID stay aligned), navigates to new workspace; rename calls `PATCH`, refreshes; delete calls `DELETE`, redirects to `/editor` if deleting the active workspace otherwise refreshes. Sidebar updated to accept `ownedProjects`/`sharedProjects` props from real data. Create dialog shows Room ID preview. `POST /api/projects` updated to accept optional custom `id`. Build verified clean.
- **08-editor-workspace-shell**: `app/editor/[roomId]/page.tsx` added as a server component — awaits `params`, calls `getCurrentIdentity()`, redirects to `/sign-in` if unauthenticated, shows `AccessDenied` for missing/unauthorized projects, and renders `WorkspaceShell`. `lib/project-access.ts` added with `getCurrentIdentity()` and `getProjectWithAccess()` helpers. `components/editor/access-denied.tsx` created with centered lock icon, message, and link back to `/editor`. `components/editor/workspace-shell.tsx` created as the client shell: workspace navbar with project name, disabled Share button, and AI sidebar toggle; `ProjectSidebar` reused with `activeProjectId` for highlighting; canvas placeholder; collapsible AI chat placeholder. `ProjectSidebar` updated with optional `activeProjectId` prop — active item renders with `bg-[var(--bg-subtle)]` background and accent text. Build and TypeScript verified clean.
- **09-share-dialog**: Share dialog wired into the workspace navbar. `getProjectWithAccess` updated to return `isOwner` boolean alongside the project summary. `GET /api/projects/[projectId]/collaborators` lists collaborators enriched with Clerk display name and avatar via `clerkClient().users.getUserList`. `POST /api/projects/[projectId]/collaborators` invites by email (owner only, 409 on duplicate). `DELETE /api/projects/[projectId]/collaborators/[collaboratorId]` removes a collaborator (owner only). `components/editor/dialogs/share-dialog.tsx` created: owners see an email invite input, a collaborator list with remove buttons, and a copy-link button with "Copied!" feedback; collaborators see the list as read-only. Share button in `WorkspaceShell` is now active, opening the dialog; `isOwner` passed from the page via `getProjectWithAccess`. Build verified clean.
- **10-liveblocks-setup**: Liveblocks real-time collaboration infrastructure wired up. `@liveblocks/node` installed. `liveblocks.config.ts` updated: `Presence` has `cursor: { x: number; y: number } | null` and `isThinking: boolean`; `UserMeta.info` has `name`, `avatar`, and `color`. `lib/liveblocks.ts` added: lazy-initialized cached `Liveblocks` node client via `getLiveblocks()`, and `getCursorColor(userId)` that deterministically hashes a user ID to one of 8 palette colors. `POST /api/liveblocks-auth` added: requires Clerk auth, verifies project access via `getProjectWithAccess`, ensures the Liveblocks room exists (creates if missing), issues a session token with user name/avatar/color, returns 403 for unauthorized access. Build verified clean.
- **11-base-canvas**: Liveblocks-backed React Flow canvas wired in. `types/canvas.ts` added with `CanvasNodeData` (label, color, shape), `CanvasNode`, and `CanvasEdge` types. `liveblocks.config.ts` updated: `Storage` declares `flow: LiveblocksFlow<CanvasNode, CanvasEdge>`. `components/editor/canvas/flow-canvas.tsx` added: `useLiveblocksFlow` with `suspense: true`, `ConnectionMode.Loose`, `fitView`, `MiniMap`, and dot-pattern `Background`. `components/editor/canvas/canvas-wrapper.tsx` added: `LiveblocksProvider` → `RoomProvider` (with `initialStorage` factory seeding empty nodes/edges maps) → `CanvasErrorBoundary` → `ClientSideSuspense` → `FlowCanvas`. `workspace-shell.tsx` updated: canvas placeholder replaced with `<CanvasWrapper roomId={project.id} />`. Build verified clean.
- **12-shape-panel**: Bottom shape panel implemented. `types/canvas.ts` extended with `NODE_SHAPES` (6 shapes with default sizes), `NODE_COLORS` (8 palette pairs), and `DEFAULT_NODE_COLOR`. `components/editor/canvas/canvas-node.tsx` added: `CanvasNodeComponent` renders all shapes as a bordered rectangle with centered label; source handles at all 4 sides, hidden by default and revealed on hover via `group-hover`. `components/editor/canvas/shape-panel.tsx` added: floating pill toolbar at bottom-center using React Flow `<Panel position="bottom-center">`; 6 draggable icon buttons set a JSON drag payload with shape name and default dimensions. `flow-canvas.tsx` updated: registers `canvasNode` type, stores `ReactFlowInstance` via `onInit`, handles `onDragOver`/`onDrop` to convert screen coords via `screenToFlowPosition` and write new nodes directly to Liveblocks storage via `useMutation`. Node IDs use shape name, timestamp, and a per-session counter. Build verified clean.
- **13-node-shape**: Proper shape rendering and drag preview implemented. `components/editor/canvas/node-shape.tsx` created: `NodeShape` component renders rectangle/pill/circle via CSS (`border-radius` only), and diamond/hexagon/cylinder via inline SVG scaled to node dimensions. `canvas-node.tsx` updated: uses `NodeShape` (fills container via `absolute inset-0`), looks up text color from `NODE_COLORS` by fill value, label rendered `z-10` above the shape. `shape-panel.tsx` updated: hides the native drag ghost via `setDragImage` with a 1×1 transparent GIF, tracks cursor via document `dragover` events, renders a portal-mounted `fixed` ghost preview (0.75 opacity, accent-primary stroke) centered on the cursor; preview cleared on `dragend`. Build verified clean.
- **14-node-editing**: Resize handles and inline label editing added to canvas nodes. `canvas-node.tsx` updated: `NodeResizer` from `@xyflow/react` shows 8 resize handles when a node is selected (min 60×40), styled as small rounded squares with `accent-primary` color; resize dimension changes flow through `useLiveblocksFlow`'s `onNodesChange` into storage automatically. Inline label editing: double-clicking a node sets `isEditing` state, renders a `nodrag nopan` textarea absolutely overlaid on the node (transparent background, centered text), local state drives the textarea value for lag-free typing while a `useMutation` propagates each keystroke to the shared Liveblocks `data.label` field via `dataLive.set("label", value)`. Editing closes on blur or Escape. `onMouseDown` and `onKeyDown` stop propagation to prevent canvas drag and pan. Build verified clean.
- **15-node-color-toolbar**: Floating color toolbar added to selected nodes. `components/editor/canvas/color-toolbar.tsx` created: `ColorToolbar` renders a pill-shaped floating panel (8 swatches from `NODE_COLORS`) positioned `calc(100% + 10px)` above the node via `bottom` absolute positioning, centered with `left: 50%; transform: translateX(-50%)`. Each `ColorSwatch` shows a hover glow via local state (`box-shadow` with `55` alpha against the pair's text color) and an active ring (`0 0 0 2px --bg-elevated, 0 0 0 3.5px textColor`). All mouse events stop propagation to prevent drag/pan. `canvas-node.tsx` updated: `updateNodeColor` mutation writes `data.color` to Liveblocks storage; `ColorToolbar` rendered conditionally when `selected`. Build verified clean.
- **16-edge-behavior**: Custom edge renderer and inline label editing implemented. `types/canvas.ts` updated: `CanvasEdgeData` interface added (`label?: string`, index sig), `CanvasEdge` data generic changed from `Record<string, never>` to `CanvasEdgeData`. `canvas-node.tsx` updated: handle `HANDLE_CLASS` now uses `!border-2 !border-[#080809]` for the dark border on white dots specified in the spec. `components/editor/canvas/canvas-edge.tsx` created: `CanvasEdgeComponent` uses `getSmoothStepPath` for right-angle routing; per-edge inline SVG `<marker>` for the arrowhead whose fill color tracks hover/selected state; wide transparent hit path (strokeWidth 20) for easy clicking without thickening the visible line; visible path at 1.5px dimmed at rest (0.4 alpha), full `#f8fafc` when hovered or selected with 0.15s CSS transition; `EdgeLabelRenderer` positioned at the `labelX`/`labelY` midpoint coordinates from `getSmoothStepPath`; double-click starts editing, blur/Enter/Escape saves, `size` attribute grows input with text; saved labels appear as pill badges; faint hint shown when active with no label; `useMutation` propagates label changes to Liveblocks storage via `storage.get("flow").get("edges").get(edgeId)`; all mouse events stop propagation to prevent canvas drag/pan. `flow-canvas.tsx` updated: `canvasEdge` registered in `edgeTypes`; `onConnect` replaced with a `useMutation`-based `handleConnect` that writes edges directly to Liveblocks storage with `type: "canvasEdge"` (Liveblocks' built-in `onConnect` does not apply `defaultEdgeOptions`, so edge type must be set in the mutation); `onEdgesChange` and `onDelete` still come from `useLiveblocksFlow`. Build verified clean.

- **17-canvas-ergonomics**: Floating control bar and keyboard shortcuts added. `components/editor/canvas/control-bar.tsx` created: pill-shaped `<Panel position="bottom-left">` with two groups — zoom controls (ZoomOut, Maximize2/fitView, ZoomIn) and history controls (Undo2, Redo2) — separated by a thin divider; zoom calls use `duration: 300` for smooth animation; undo/redo wired to `useUndo`/`useRedo`/`useCanUndo`/`useCanRedo` from `@liveblocks/react`; disabled buttons rendered with `opacity-40 cursor-not-allowed`. `hooks/useKeyboardShortcuts.ts` created: generic hook receiving `ReactFlowInstance<N,E> | null`, `undo`, `redo`; listens on `window` keydown; skips `input`, `textarea`, and `contenteditable` targets; handles `+`/`=` zoom in, `-` zoom out, `Cmd/Ctrl+Z` undo, `Cmd/Ctrl+Shift+Z` redo, `Cmd/Ctrl+Y` redo. `flow-canvas.tsx` updated: `KeyboardShortcutsHandler` inner component uses `useReactFlow()` (inside React Flow context) and calls `useKeyboardShortcuts`; `useUndo`/`useRedo` called in `FlowCanvas` and passed as props; `<ControlBar />` and `<KeyboardShortcutsHandler />` rendered inside `<ReactFlow>`. Build verified clean.

- **18-starter-templates**: Starter template library added. `components/editor/starter-templates.ts` created: `CanvasTemplate` interface, helper functions `n()`/`e()` for concise node/edge construction, and `CANVAS_TEMPLATES` array with three pre-built templates — Microservices Architecture (API gateway + 3 services + 2 DBs), CI/CD Pipeline (7-stage linear flow), Event-Driven System (2 producers + broker + 3 workers + event store). All nodes use shared `CanvasNode`/`CanvasEdge` types and `NODE_COLORS` palette. `components/editor/starter-templates-modal.tsx` created: Dialog with scrollable grid of `TemplateCard` components; each card renders a `DiagramPreview` (pure SVG, bounds-calculated and scaled to 280×160 viewport, edges as lines between node centers, nodes drawn per shape — diamond/hexagon as polygons, circle as ellipse, cylinder as ellipse+rect, rectangle/pill as `<rect rx>`); Import button calls `onImport(template)`. `flow-canvas.tsx` updated: accepts `templatesOpen`/`onTemplatesClose` props; `loadTemplate` Liveblocks mutation clears all nodes and edges then writes template nodes/edges as `LiveObject`s; `handleTemplateImport` calls the mutation, closes the modal, then calls `fitView` after 150ms; `<StarterTemplatesModal>` rendered outside `<ReactFlow>`. `canvas-wrapper.tsx` updated: passes `templatesOpen`/`onTemplatesClose` through to `FlowCanvas`. `workspace-shell.tsx` updated: `templatesOpen` state, "Templates" button with `LayoutTemplate` icon added to the navbar before "Share". Build verified clean.

- **19-presence-avatars-cursors**: Presence avatars and live cursors added to the editor canvas view. `liveblocks.config.ts` updated: `isThinking` renamed to `thinking` per spec. `canvas-wrapper.tsx` updated: `initialPresence` updated accordingly. `components/editor/canvas/presence-avatars.tsx` created: `PresenceAvatars` uses `useOthers()` to get collaborators (filtered to exclude current Clerk user via `useUser()`), renders up to 5 overlapping avatars with color ring (image or initials fallback), a +N overflow chip, a conditional divider, and the Clerk `UserButton` for the current user. `components/editor/canvas/live-cursors.tsx` created: `LiveCursors` uses `useOthers()` + `useViewport()` to project Liveblocks flow-coordinate cursor positions to canvas-container screen coordinates, renders a colored SVG pointer with name badge for each other participant. `flow-canvas.tsx` updated: `useUpdateMyPresence` imported; `handleMouseMove` broadcasts cursor flow position on React Flow's `onMouseMove`; `handleMouseLeave` clears cursor to null; `<Panel position="top-right">` renders `<PresenceAvatars>`; `<LiveCursors>` rendered as a sibling inside `<ReactFlow>`. Editor home navbar unchanged. Build verified clean.

- **20-ai-sidebar-shell**: AI sidebar extracted into its own component and built out. `components/editor/ai-sidebar.tsx` created: floating `<aside>` fixed to the right edge (`top-12`, `z-30`, `w-80`), slides in from the right via `translate-x-full`/`translate-x-0` transition, `bg-[var(--bg-base)]/95 backdrop-blur-sm` background with left border and `shadow-xl`. Header has Bot icon (`text-[var(--accent-ai-text)]`), "AI Workspace" title, "Collaborate with Ghost AI" subtitle, and a close button. Two shadcn Tabs: "AI Architect" and "Specs" — active state styled with `bg-[var(--accent-ai)] text-white`, inactive with `text-[var(--text-muted)]`. AI Architect tab: scrollable chat area with empty state (Bot icon, description, three starter prompt chips using `bg-subtle` and `text-[var(--accent-ai-text)]`); user messages right-aligned with `bg-[var(--accent-primary-dim)] border-[var(--accent-primary)]/50 border-2`; assistant messages left-aligned with `bg-[var(--bg-elevated)] border-[var(--border-default)]`; auto-resizing textarea (72px–160px), Send button (`bg-[var(--accent-primary)]`), Enter to send/Shift+Enter for newline. Specs tab: "Generate Spec" button (`bg-[var(--accent-primary)]`), demo spec card with FileText icon, title, snippet, and disabled Download action (`bg-[var(--bg-elevated)] border-[var(--border-default)]`). `workspace-shell.tsx` updated: inline `<aside>` placeholder removed, `<AiSidebar>` rendered as a sibling after the main content area. Build verified clean.

- **21-canvas-autosave**: Canvas autosave and load implemented. `@vercel/blob` installed. `GET /api/projects/[projectId]/canvas` reads `canvasJsonPath` from Prisma; returns 204 if none, fetches and proxies JSON from Vercel Blob otherwise. `PUT /api/projects/[projectId]/canvas` uploads canvas JSON to Vercel Blob at `canvas/{projectId}.json` (overwriting prior snapshot), then stores the returned blob URL in `project.canvasJsonPath`. Both routes use a shared `checkAccess` helper that verifies owner OR collaborator access via `getCurrentIdentity()`. `hooks/use-canvas-autosave.ts` created: debounces writes (2 s default), fires only when `enabled` is true, tracks `"idle" | "saving" | "saved" | "error"` status. `flow-canvas.tsx` updated: `projectId` prop added; `writeToStorage` mutation writes fetched nodes/edges as `LiveObject`s; `useEffect` on mount checks if the room is empty and loads the saved blob if so, then sets `loadComplete = true`; autosave fires only after `loadComplete`; `SaveStatusIndicator` renders in a `<Panel position="top-left">` showing "Saving…", "Saved", or "Save failed". `canvas-wrapper.tsx` updated: `projectId` prop threaded through to `FlowCanvas`. `workspace-shell.tsx` updated: passes `projectId={project.id}` alongside `roomId`. `prisma/models/project.prisma` had `canvasJsonPath String?` already — no migration required. Build verified clean.

- **22-trigger-setup**: Trigger.dev v4 wired in. `@trigger.dev/sdk@^4.4.6` installed. `trigger.config.ts` created at project root with `project` (via `TRIGGER_PROJECT_ID` env var), `dirs: ["./trigger"]`, and `maxDuration: 300`. `trigger/ai-design.ts` exports `aiDesignTask` (id: `ai-design-generation`) — stub for AI architecture generation from prompt into Liveblocks room. `trigger/spec-generation.ts` exports `specGenerationTask` (id: `spec-generation`) — stub for Markdown spec generation from canvas JSON into Vercel Blob + Prisma. `trigger:dev` script added to `package.json`. Build and TypeScript verified clean.

- **22-design-agent-api**: Design generation backend wired up. `TaskRun` Prisma model added (`runId` unique, `projectId`, `userId`, `createdAt`; index on `runId`; compound index on `userId` + `projectId`); migration `20260627132219_add_task_run` applied. `trigger/design-agent.ts` created: `designAgentTask` (id: `design-agent`) accepts `{ prompt, roomId }` payload, logs input, returns stub output — no AI logic yet. `POST /api/ai/design` accepts `{ prompt, roomId, projectId }`, verifies Clerk auth and owner-or-collaborator access, triggers `design-agent` task via `tasks.trigger`, creates a `TaskRun` record, returns `{ runId }`. `POST /api/ai/design/token` accepts `{ runId }`, verifies Clerk auth and `TaskRun.userId` ownership, issues a Trigger.dev public token scoped to that run with 1h expiry. Prisma client regenerated. Build verified clean.

- **23-design-agent-logic**: Full AI design agent implemented end-to-end. `liveblocks.config.ts` updated: `RoomEvent` now types `{ type: "ai-status"; message: string; phase: "start" | "thinking" | "writing" | "complete" | "error" }` for type-safe broadcasts. `trigger/design-agent.ts` fully implemented: reads existing canvas via `mutateFlow` (read pass), calls `generateObject` with `google("gemini-2.0-flash")` and a Zod schema producing `nodesToAdd`, `nodesToUpdate`, `nodeIdsToDelete`, `edgesToAdd`, `edgeIdsToDelete`, `summary`; applies design back to canvas via a second `mutateFlow` write pass; broadcasts AI status events at each phase (start/thinking/writing/complete/error) via `getLiveblocks().broadcastEvent`; sets/clears AI virtual presence via `getLiveblocks().setPresence` (userId: `ghost-ai`, color: `#6457f9`) with `ttl: 300` while thinking and `ttl: 5` on complete. `components/editor/canvas/flow-canvas.tsx` updated: `useEventListener` from `@liveblocks/react` subscribed to `ai-status` events; `AiStatusBanner` component renders in the `top-left` Panel showing the current phase message, auto-hides after 4s on complete/error. `components/editor/ai-sidebar.tsx` fully wired: `projectId` and `roomId` props added; `submit()` now calls `POST /api/ai/design` then `POST /api/ai/design/token`; `RunTracker` component uses `useRealtimeRun` to watch the run and calls `onComplete`/`onError` callbacks; loading state shown as spinner + "Designing…" bubble; input disabled while processing. `components/editor/workspace-shell.tsx` updated: passes `projectId={project.id}` and `roomId={project.id}` to `AiSidebar`. Build verified clean.

- **24-ai-presence-state**: Shared AI activity indicators implemented. `types/tasks.ts` created: `aiStatusMessageSchema` (Zod, `text` nullish string) and `AiStatusMessage` type for feed payload validation. `liveblocks.config.ts` updated: `aiStatusFeed: LiveObject<{ text: string | null }>` added to `Storage`; `thinking: boolean` confirmed in `Presence`. `LiveblocksProvider` and `RoomProvider` lifted from `canvas-wrapper.tsx` to `workspace-shell.tsx` so both `CanvasWrapper` and `AiSidebar` share one room connection; `initialStorage` includes `aiStatusFeed: new LiveObject({ text: null })`; `initialPresence` includes `thinking: false`. `canvas-wrapper.tsx` stripped of provider imports — now only wraps `CanvasErrorBoundary` + `ClientSideSuspense`. `ai-sidebar.tsx` updated: `useOthers` detects `ghost-ai` with `thinking: true`; `useStorage` reads `aiStatusFeed.text` validated via `aiStatusMessageSchema`; header shows animated spinner + live status text while AI is active; `isProcessing` includes shared state so input/send button disable for all room participants. `live-cursors.tsx` updated: `Loader2` spinner shown in cursor name badge when `other.presence.thinking === true`. `trigger/design-agent.ts` updated: `updateAiFeed` helper writes text to `aiStatusFeed` via `liveblocks.mutateStorage`; `broadcastStatus` calls `updateAiFeed` with the phase message on active phases and `null` on complete/error so new joiners see current status. Build verified clean.

- **25-sidebar-chat-feed**: Real-time room chat added to the AI sidebar using a separate `aiChatFeed` Liveblocks feed. `types/tasks.ts` updated: `chatMessageSchema` (Zod, fields: `id`, `sender`, `role`, `content`, `timestamp`) and `ChatMessage` type added. `liveblocks.config.ts` updated: `LiveList` imported; `aiChatFeed: LiveList<LiveObject<ChatMessage>>` added to `Storage`. `workspace-shell.tsx` updated: `LiveList` imported from `@liveblocks/client`; `aiChatFeed: new LiveList([])` added to `initialStorage`. `components/editor/ai-sidebar.tsx` updated: `useMutation`, `useSelf` imported from `@liveblocks/react`; `LiveObject` imported from `@liveblocks/client`; `chatMessageSchema`/`ChatMessage` imported; `rawChatMessages` subscribed via `useStorage`, each message validated with `chatMessageSchema.safeParse` before rendering; `sendChatMutation` uses `useMutation` to push a new `LiveObject` to `aiChatFeed` with id/sender/role/content/timestamp; `sendChat` function calls the mutation and shows `chatError` on failure, clears input on success; "Chat" tab added between "AI Architect" and "Specs" tabs — shows empty state, ordered message list (sender + formatted timestamp + content), a dedicated Textarea + Send button using the same input pattern, and an inline error message on failure; `formatTime` helper formats timestamps as locale time string. `aiChatFeed` is kept strictly separate from `aiStatusFeed`. Build verified clean.

- **26-ai-chat-functional**: AI sidebar wired up end-to-end with real-time run tracking and Liveblocks-backed message persistence. `liveblocks.config.ts` updated: `aiArchitectFeed: LiveList<LiveObject<ChatMessage>>` added to `Storage` — separate from `aiChatFeed` (room chat). `workspace-shell.tsx` updated: `aiArchitectFeed: new LiveList([])` added to `initialStorage`. `POST /api/ai/design` updated: issues a Trigger.dev public token for the run after creating the `TaskRun` record and returns `{ runId, publicToken }` in a single response, eliminating the separate token call. `components/editor/ai-sidebar.tsx` fully wired: local `messages` state replaced with `useStorage` subscription to `aiArchitectFeed` (validated via `chatMessageSchema`); `pushArchitectMessage` mutation pushes user and AI messages as `LiveObject<ChatMessage>` so all collaborators see the same conversation; `submit()` pushes user message to feed before calling the API, reads `{ runId, publicToken }` from the single design endpoint response; `handleRunComplete` and `handleRunError` push final AI messages via the mutation; user chat bubbles styled with `#62C073` green background and `#0F2E18` dark text; AI bubbles use `bg-[var(--bg-elevated)] text-[var(--text-primary)]`; submit button uses `#62C073` green (`GREEN_ACCENT` constant); compact status strip added directly above the input — rendered only when `runState !== null || isAiThinking`, shows `sharedStatusText` or "Ghost AI is working..." with animated spinner in green accent. Canvas updates remain Liveblocks-only (no manual sync). Build and TypeScript verified clean.

## In Progress

- None.

## Next Up

- Next feature spec.


## Open Questions

- Add unresolved product or implementation questions here.

## Architecture Decisions

- Add decisions that affect the system design or data model.

## Session Notes

- Add context needed to resume work in the next session.
